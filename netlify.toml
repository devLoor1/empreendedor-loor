[build]
  command = "bun install && bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
  BUN_VERSION = "1.3"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
